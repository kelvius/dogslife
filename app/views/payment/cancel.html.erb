<!DOCTYPE html>
<html>
<head>
  <title>Payment Cancelled</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <div class="jumbotron text-center">
      <h1 class="display-4">Payment Cancelled</h1>
      <p class="lead">Your payment could not be processed at this time.</p>
      <hr class="my-4">
      <p>Please check your payment details and try again. If you continue to experience issues, please contact our support team.</p>
      <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">Attempted Payment Details</h4>
        <p>Transaction ID: <%= @payment_intent.id if @payment_intent %></p>
        <p>Amount: $<%= @payment_intent.amount_to_be_charged.to_i / 100.0 if @payment_intent %></p>
        <p>Status: <%= @payment_intent.status.capitalize if @payment_intent %></p>
        <!-- Add any additional details you wish to include -->
      </div>
      <%= link_to 'Try Again', new_payment_path, class: 'btn btn-primary btn-lg', role: 'button' %>
      <%= link_to 'Return Home', root_path, class: 'btn btn-secondary btn-lg', role: 'button' %>
    </div>
  </div>

  <!-- Optional JavaScript -->
  <!-- jQuery and Bootstrap Bundle (includes Popper) -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
