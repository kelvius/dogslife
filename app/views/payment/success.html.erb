<!DOCTYPE html>
<html>
<head>
  <title>Payment Success</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <div class="jumbotron text-center">
      <h1 class="display-4">Thank You!</h1>
      <p class="lead">Your payment has been processed successfully.</p>
      <hr class="my-4">
      <p>We are thrilled to let you know that your adoption process is complete. A new furry friend is waiting for you!</p>
      <div class="alert alert-success" role="alert">
        <h4 class="alert-heading">Payment Details</h4>
        <p>Transaction ID: <%= @payment_intent.id %></p>
        <p>Amount: $<%= @payment_intent.amount_received.to_i / 100.0 %></p>
        <p>Status: <%= @payment_intent.status.capitalize %></p>
        <!-- Add any additional details you wish to include -->
      </div>
      <%= link_to 'Adopt Another Dog', dogs_path, class: 'btn btn-primary btn-lg', role: 'button' %>
      <%= link_to 'Return Home', root_path, class: 'btn btn-secondary btn-lg', role: 'button' %>
    </div>
  </div>

  <!-- Optional JavaScript -->
  <!-- jQuery and Bootstrap Bundle (includes Popper) -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
